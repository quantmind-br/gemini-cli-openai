# Gemini CLI OpenAI Docker Environment Variables
# Essential configuration for application startup

# Port for the application to listen on
PORT=3000

# Redis connection URL for caching
REDIS_URL=redis://redis:6379

# Dashboard Authentication (required for web dashboard access)
# Set these credentials to enable the configuration dashboard at /dashboard
# All other settings can be configured through the web dashboard interface
DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=your-secure-password-here

# ========================================
# NOTE: The following variables can be configured via the web dashboard
# at http://localhost:3000/dashboard instead of adding them here:
#
# - GCP_SERVICE_ACCOUNT (OAuth2 credentials - upload JSON file)
# - OPENAI_API_KEY (Optional API authentication)
# - GEMINI_PROJECT_ID (Google Cloud Project ID)
# - ENABLE_FAKE_THINKING (Synthetic thinking output)
# - ENABLE_REAL_THINKING (Native Gemini reasoning)
# - STREAM_THINKING_AS_CONTENT (DeepSeek R1 style output)
# - DASHBOARD_USERNAME/PASSWORD (Can be changed via dashboard)
#
# Using the dashboard is the recommended approach for easier configuration
# and better security management.
# ========================================